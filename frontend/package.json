{
  "name": "web-boilerplate-vite",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite & (cd ../generate-embeddings && node server.js)",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  }
}